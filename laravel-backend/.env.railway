APP_NAME="Chikugin CRI"
APP_ENV=production
APP_KEY=base64:mMMdejWED7RnXUQYo23TiGtbu+tV0WDnwaaAzxdMO44=
APP_DEBUG=false
APP_URL=${RAILWAY_PUBLIC_DOMAIN}

LOG_CHANNEL=stack
LOG_LEVEL=error

# Railway PostgreSQL - 環境変数から自動的に取得
DB_CONNECTION=pgsql
DATABASE_URL=${DATABASE_URL}

# CORS設定
CORS_ALLOWED_ORIGINS=${FRONTEND_URL}

BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120

# --- Storage (Images/Files) ---
# Public URLs for files saved to the `public` disk
# Current test origin (Railway): adjust if CDN is introduced later
PUBLIC_STORAGE_URL=https://heroic-celebration-production.up.railway.app/storage
# On Railway (dev), keep empty/false to use storage/app/public + symlink
PUBLIC_DISK_IN_PUBLIC=

# Enable debug endpoints in dev to verify storage and rate limits
ENABLE_DEBUG_ENDPOINTS=true
